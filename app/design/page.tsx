import DesignReference from '@/components/ui/DesignReference';

export const metadata = {
  title: 'Sandbars — Design Reference',
};

export default function DesignPage() {
  return <DesignReference />;
}
